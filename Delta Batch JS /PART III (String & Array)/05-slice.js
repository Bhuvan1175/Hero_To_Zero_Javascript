// slice 
// Returns a part of the original string as a new string.
// The slice() method extracts a part of a string and returns a new string.

let msg="apnacollege";
console.log(msg.slice(4,7));
console.log(msg.slice(4));

console.log(msg.length);
console.log(msg.slice(-2));