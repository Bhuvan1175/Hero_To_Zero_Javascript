// String interpolation in JavaScript refers to the process of embedding expressions or variables within a string. This can be achieved using template literals, which are strings delimited by backticks [ ` `].

let str="Bhuvan Bam";
let result=str.length;
console.log(`Length of ${str} is ${result} `);
